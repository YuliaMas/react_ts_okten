export type IUserFormType = {
    name: string,
    username: string,
    password: string,
    email: string,
    age: number,
    id?: number
};

 export type IFormType = {
    title: string,
    body: string,
    userId: number,
}